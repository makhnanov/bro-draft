# Функционал распознавания экрана

## Описание

Новый функционал позволяет создавать последовательности автоматических действий на основе распознавания изображений на экране. Это полезно для автоматизации повторяющихся действий в приложениях.

## Как использовать

### 1. Создание последовательности

1. Откройте страницу **Recognition** в приложении
2. Переключитесь на вкладку **Screen**
3. Нажмите кнопку **"Создать последовательность"**

### 2. Добавление шагов

1. В созданной последовательности нажмите **"Добавить шаг"**
2. Откроется полноэкранный режим выбора области
3. Выделите мышкой область экрана, которую нужно распознавать (например, кнопку или элемент интерфейса)
4. Отпустите кнопку мыши - область будет сохранена как шаг
5. Повторите процесс для добавления дополнительных шагов

### 3. Запуск последовательности

1. Нажмите кнопку **▶ (Play)** рядом с нужной последовательностью
2. Приложение будет:
   - Искать первое сохранённое изображение на текущем экране
   - При нахождении - кликнет по центру этого изображения
   - Перейдёт к следующему шагу
   - Повторит процесс для всех шагов последовательности

### 4. Управление последовательностями

- **Удаление шага**: нажмите ❌ рядом с шагом
- **Удаление последовательности**: нажмите 🗑️ рядом с названием последовательности

## Технические детали

### Алгоритм поиска изображения (Оптимизированный)

**Многоуровневая оптимизация для быстрого поиска:**

1. **Параллельная обработка** - использование всех ядер процессора через библиотеку Rayon
2. **Шаг поиска** - проверка каждой 2-й позиции (step=2) вместо каждой, что ускоряет поиск в 4 раза
3. **Быстрая фильтрация** - сначала проверяются только 5 ключевых точек (углы + центр)
4. **Прореживание пикселей** - проверка каждого 2-го пикселя в найденной области (check_step=2)
5. **Ранний выход** - остановка поиска сразу после нахождения первого совпадения
6. **Параметры**:
   - Порог различия: **50 единиц** на канал RGB (более гибкое распознавание)
   - Требуемое совпадение: **90%** проверенных пикселей
   - Поддержка прозрачности (альфа-канал)
   - Скорость движения курсора: **в 3 раза быстрее** стандартной

**Производительность:** На типичном экране 1920x1080 поиск занимает **0.1-0.5 секунды** (в зависимости от размера искомого изображения и его позиции)

### Как это работает технически

1. **Захват скриншота**: При запуске последовательности делается скриншот текущего состояния экрана с помощью команды `capture_monitor_screenshot`
2. **Поиск изображения**: Сохраненное изображение шага ищется на скриншоте попиксельно с помощью команды `find_image_on_screen`
3. **Вычисление координат**: Найденные координаты на мониторе преобразуются в абсолютные экранные координаты с учетом позиции монитора
4. **Клик**: Выполняется клик по центру найденного изображения

### Системные требования

- Linux: требуется `xdotool` для эмуляции кликов
- Разрешены права на создание скриншотов

### Новые Tauri команды

- `capture_monitor_screenshot(monitor_index)` - захват скриншота конкретного монитора без UI
- `find_image_on_screen(screen_image, target_image, target_width, target_height)` - поиск изображения на скриншоте

## Примеры использования

### Автоматизация веб-формы
1. Создайте последовательность
2. Добавьте шаги для каждого поля формы (по скриншоту кнопки/поля)
3. Запустите - приложение автоматически кликнет по всем элементам

### Автоматизация игровых действий
1. Сохраните скриншоты кнопок/элементов игры
2. Создайте последовательность кликов
3. Запустите для повторения действий

## Хранение данных

- Все последовательности сохраняются в localStorage браузера
- Ключ: `screen-recognition-sequences`
- Формат: JSON с base64-кодированными изображениями

## Ограничения

- Поиск изображения может занимать время на больших экранах
- Работает только с видимыми элементами на экране
- Чувствительность к изменениям интерфейса (цвет, размер элементов)
